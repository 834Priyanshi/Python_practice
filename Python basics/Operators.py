#Write a Python program that takes the base and height as input from the user and calculates the area of the triangle.
# base = float(input("Enter the base of the triangle:"))
# height = float(input("Enter the height of the triangle:"))
# area = (base*height)/2
# print("The area of the triangle is:", area)

a=10
b=5
print("a>b", a>b)
print("a<b",a<b)
print("a==b",a==b)
print("a!=b",a!=b)
